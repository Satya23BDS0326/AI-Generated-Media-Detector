services:
  - type: web
    name: ai-generated-media-detector
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m shiny run --host 0.0.0.0 --port 10000 app.py